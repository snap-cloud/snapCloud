<% content_for('title', locale.get('follower_users')) %>
<h1 class="mb-3"><%= locale.get('follower_users') %></h1>

<%
render(
    'views.grid_bs',
    {
        class = 'users',
        item_type = 'user_bs',
        items_per_row = 3,
        with_search = false,
        show_if_empty = true,
    }
)
%>
