<h1><%- locale.get('teacher_title') %></h1>
<div class="buttons">
    <a class="pure-button" href="bulk"><%- locale.get('bulk_tile') %></a>
</div>

<section>
    <%
    render(
        'views.grid',
        {
            title = locale.get('your_learners'),
            item_type = 'user',
            items_per_row = 1,
            items_per_page = 150,
            show_if_empty = true,
            skip_pagination = true,
            num_pages = 1
        }
    )
    %>
</section>
