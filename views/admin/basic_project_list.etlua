<% content_for('title', locale.get(page_title)) %>

<h1><%= locale.get(page_title) %></h1>

<%
render(
    'views.grid',
    {
        items = items,
        item_type = 'project',
        items_per_page = 24,
    }
) %>
